---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: tenants.muninn.io
spec:
  group: muninn.io
  names:
    kind: Tenant
    listKind: TenantList
    plural: tenants
    shortNames:
    - tn
    singular: tenant
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tenantID
      name: Tenant ID
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.namespace
      name: Namespace
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Tenant is the schema for the tenants API.
          It represents a single tenant in the platform.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TenantSpec defines the desired state of Tenant.
            properties:
              displayName:
                description: DisplayName is a human-friendly name for the tenant.
                type: string
              tenantID:
                description: |-
                  TenantID is the stable, globally unique identifier for the tenant.
                  Immutable once set.
                type: string
            type: object
          status:
            description: TenantStatus defines the observed state of Tenant.
            properties:
              cloudResouces:
                description: CloudResources tracks external resources provisioned
                  for this tenant.
                properties:
                  identityClientID:
                    description: IdentityClientID is the client application ID registered
                      with the identity provider.
                    type: string
                  identityDomain:
                    description: IdentityDomain is the tenant-scoped domain for the
                      identity provider.
                    type: string
                  identityPoolARN:
                    description: IdentityPoolARN is the ARN or full resource identifier
                      of the identity pool.
                    type: string
                  identityPoolID:
                    description: IdentityPoolID is the ID of the identity provider
                      pool for this tenant.
                    type: string
                  storageBucketARN:
                    description: StorageBucketARN is the ARN or full resource identifier
                      of the storage bucket.
                    type: string
                  storageBucketName:
                    description: StorageBucketName is the name of the storage bucket
                      provisioned for this tenant.
                    type: string
                type: object
              conditions:
                description: Conditions represent the latest overvations of the resource
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              namespace:
                description: |-
                  Namespace is the dedicted kubernetes namespace for this tenant.
                  Format: tenant-<tenantID>
                type: string
              phase:
                description: Phase indicates the current lifecycle phase of the tenant.
                enum:
                - Pending
                - Provisioning
                - Ready
                - Suspended
                - Deleting
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
